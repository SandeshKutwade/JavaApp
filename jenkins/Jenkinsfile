pipeline{
    agent {label 'mavenjob'}
    tool {maven 'maventool'}
    stages {
        stage ('checkout') {
            steps {
                checkout scm
            }
            
        }
        stage ('Build'){
            steps{
                sh 'mvn clean install'
            }
        }
            
        }
    }